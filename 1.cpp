#include <iostream>
#include <cmath>
using namespace std;
int main ()
{
	int a,b,c,d,e;
	if (a>b && a>c && a>d && a>e){ 
		cout<<"a is max";
		} else if (b>a && b>c && b>d && b>d){
			cout<<"b is max";
		} else if (c>a && c>b && c>d && c>e){
			cout<<"c is max";
		} else if (d>a && d>b && d>c && d>e){
			cout<<"d is max";
		} else if (e>a && e>b && e>c && e>d){
			cout<<"e is max"
		}
	return 0;

}